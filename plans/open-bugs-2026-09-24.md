# Open bugs, 2026-09-24

- **As of:** `1f8cb02`, the tip of `feat/adr-007-009-cache-redesign` (PR #189), which
  carries ADR-007, ADR-008, ADR-009 and ADR-011. Nothing here is on `main` yet.
- **Scope:** every open issue in buckaroo-data/tallyman, checked against that commit.
  #190 to #240 were reproduced there, by a probe or by reading the code. The older
  issues were re-read against the code, with a probe where one was cheap.
- **Status words:**
  - **confirmed**: the bug reproduces at `1f8cb02` as filed.
  - **changed**: the mechanism was rewritten and the bug survives in a different form, described in the row.
  - **new**: filed on 2026-09-24.
- **Priority:**
  - **P1**: rows, pins or edges come out wrong without an error.
  - **P2**: a request fails, hangs, or the UI shows the wrong state.
  - **P3**: leaks, misleading messages, missing hints and cleanup.

## P1: wrong data without an error

| # | status | what goes wrong |
|---|---|---|
| #229 | new | A tracked child and a pinned child with the same query are one entry, so the first build's edge wins. A pinned alias can then move when its parent is revised. |
| #228 | new | `_raw_parquet_read_check` exempts every file under `compute_cache/`, so a recipe can read an entry's snapshot by hash. It records no parent and never goes stale. |
| #232 | new | An unfaithful heal of a source version is blamed on the recipe. A corrupt clone is healed from, pinned, and not repaired by a re-import. |
| #208 | confirmed | An unfaithful heal of a worthy parent changes its cheap children's rows under their hashes, and only the parent is flagged. |
| #209 | confirmed | The expanded-build marker does not record the project root, so a copied project reads the old project's paths. A comment adds that the marker is trusted even when the expanded folder is gone. |
| #200 | confirmed | `full_diff` treats `__row_order` as data, so one inserted row shows every later row as changed. |
| #13 | confirmed, wider than filed | The diff's membership sentinel is the first non-key column, so a null in that column puts a row in the wrong bucket. Ordinary data triggers it, not only key-only diffs. |
| #206 | confirmed | The snapshot writer drops any column named `__row_order_right`, including one the author made. |
| #205 | confirmed (code) | The canonical sort (ADR-008's fixed row order for a worthy entry) leaves nested columns out of the tie-break, so rows tied on every sortable column can still come out in different orders. |
| #142 | confirmed | The CSV reader, which runs at import, null-fills short rows instead of raising. ADR-005 deferred it. |
| #169 | confirmed | Primary-key inheritance finds the parent by an alphabetical alias scan and checks the key by column name only, so a diff can join on a non-unique key. |
| #172 | changed | Entry sessions are fixed. Diff sessions are still keyed `diff-<a12>-<b12>` with no project, and source hashes are the same across projects, so one project can be served another's diff session. |

## P2: failures, hangs, and a UI that shows the wrong state

| # | status | what goes wrong |
|---|---|---|
| #190 | confirmed | `put_code` and `api_promote_diff` build on the companion's event loop, so the whole UI freezes while one waits for the project lock. |
| #201 | confirmed | `reload_project_sessions` posts `/reload_expr` once per catalog entry (source versions included), one after another, on the event loop. |
| #202 | confirmed | Every grid open posts `/load_expr`. Concurrent opens load twice, and a promoted diff re-runs Buckaroo's stats on every open. |
| #203 | confirmed | An unfaithful heal runs its checks, and the companion's forced Buckaroo reload, while holding the project lock. `_heal_a_source` is a second place this happens. |
| #233 | new | A recipe's alias reads resolve against the active-project file, not the project being built. An MCP session or a companion route on another project gets a refusal that names the wrong project's file. |
| #39 | confirmed | The active-project file outranks `TALLYMAN_PROJECT` and `mcp --project`, so an MCP process cannot be pinned to a project. Pinning it would also make #233 the normal case. |
| #164 | confirmed | The post-processing validator dry-runs on a fixed `{a, b}` table, which forces a guard that hides shape errors and blocks diff-scoped filters. |
| #240 | new | Alias, notebook and `config.json` writes load, change and replace the file without the project lock. If a second writer loads the file before the first one's replace lands, the first change is lost. |
| #238 | new | `catalog_recalc` with a source entry as a root fails, because the replay runs the generated `read_project_file` outside the import, and the failure is recorded against good data. |
| #227 | new | `catalog_import_source` lets a CSV reader error, or a clone that fails its digest check, escape. Nothing is recorded, and the message names the clone and `tallyman_read_csv`. |
| #224 | new | Importing a parquet file with a `fixed_size_binary` or UUID column fails with a 3,000-character KeyError traceback that names no column. |
| #235 | new | The SPA has no listener for `project_reset`, `unfaithful_heal`, `entry_added` or `alias_changed`, so an open page goes stale. |
| #199 | confirmed | `assert_joinable` refuses semi and anti join chains over entries, though they add no right-side columns. |
| #161 | confirmed | Auto-recalc rebuilds dependents with no prompt, so they show "no prompt" from v2 on. |
| #111 | confirmed, wider than filed | The alias is moved before the entry config is carried forward. This happens in four paths (revise, `PUT /code`, recalc, import). In recalc, a carry-forward failure escapes the build's `try`. |
| #170 | confirmed | Buckaroo is given `project_root = artifacts/`, but klasses live under `artifacts/catalog/`, so project stats and grid post-processing never load. |
| #177 | confirmed | Every klass reload wipes the entry's stat cache, which forces a cold recompute. |
| #156 | changed | The global session lock is gone. A load longer than `10 s + 1 s per million rows` still reports a timeout although Buckaroo finishes it, and same-hash opens are not merged. |
| #34 | changed | A timeout now returns a typed status with a retry. Loads are still synchronous and not deduplicated (see #202). |
| #37 | changed | An alias gets a distinct `no_build` status, but `GET /api/session/{alias}` still does not resolve aliases. |
| #1 | confirmed | `extra_grid_config` from the display config never reaches `/load_expr`. Sending it would also trigger #202's reload on every open. |
| #45 | confirmed (not re-measured) | The diff page computes the full diff synchronously on first open, then waits up to 30 s on `/load_expr`. |
| #210 | confirmed | Dropping `_ResultPlan.loaded` frees nothing. xorq's unbounded `translate_from_yaml` cache keeps a backend per `load_expr`, so the fix has to be on tallyman's side. |
| #22 | changed | A checkpoint no longer lists `compute_cache/`, but it still runs under the lock before the response, and its cost grows with the entry count. The bullpen only grows. |
| #186 | confirmed | The project lock blocks with no timeout and no busy indicator, and page heals and imports take it. |
| #23 | confirmed | The marimo export omits post-processing, summary stats, charts and display config. |
| #43 | confirmed | Exported cells set `TALLYMAN_PROJECT` but not `TALLYMAN_HOME`. |
| #44 | confirmed | Exported cells re-run the full query and hand the whole result to `mo.ui.table`. |

## P3: leaks, messages, hints, cleanup

| # | status | what goes wrong |
|---|---|---|
| #225 | new | A failed import leaves its clone in `data/.cas/` and its snapshot in `result_cache/`. Nothing ever deletes the clone. |
| #226 | new | A writer killed mid-write leaves a `.tmp` in `result_cache/` or `data/.cas/` that the Cache page never lists and nothing deletes. |
| #230 | new | Every failed build leaves a `tallyman_expr_<uuid>.py` in the OS temp dir. This machine has 4,163 of them. |
| #35 | confirmed (partly fixed) | A build that raises cleans up its entry dir. A killed one still leaves a dir without a manifest, and nothing sweeps it. |
| #239 | new | Re-importing a version whose clone is lost does not restore the clone while its snapshot exists, so the version stays pinned. |
| #234 | new | The import error for an older version's bytes names reset commands that don't exist (`tallyman reset`, `catalog_reset_to`). |
| #38 | confirmed | The customization tools (add stat, klass, post-processing) return errors without `record_error`, so they get no `error_id` and don't appear in `/api/errors`. |
| #40, #42, #107 | confirmed | Missing build-error hints: `try_cast` raises an Arrow cast error; `window(partition_by=)` raises TypeError; a window over a `lag()` column fails to plan. |
| #41 | confirmed | An aggregate over an ordered `ibis.window()` covers the whole partition (`[3,3,3]`, not the running `[1,2,3]`), and no lint catches it. |
| #236 | new | ADR-011 leftovers: dead helpers (`recon_cas_path`, `_spec_sig`, `_polars_overrides`), text describing ordered copies and identity modes, and no test for the callable reader-option refusal. |
| #110 | confirmed | `carry_forward_entry_config`'s guard branches have no tests. |
| #138 | confirmed | `packages/embed/` is still in the tree and nothing builds it. |
| #157 | changed | The stat cache now hits across a Buckaroo restart. What remains is #177's wipe and Buckaroo's cache log being thrown away. |

## Features and design questions still open

#237 (`catalog_list` should report an alias's kind), #185 (the purity verdict is not
recorded or passed to children), #187 (float aggregates over parent row-group
layout: only the research list remains), #188 (build every diff as an entry), #146
(re-scoped to window `order_by` and ordered aggregates), #150 (exact decimals), #151
(pattern-based parents), #125 (hard-fail nondeterministic recipes), #140 (remove
`api_data`), #4 (`api_data` still round-trips through pandas and JSON), #10 (cache
eviction), #60 (log levels), #78 (per-column fingerprints), #106 (per-project venvs),
#2 (`catalog_train`: fitting works in a recipe, the storyboard beats are missing), #3,
#6, #7 (scratch entries are never recalculated, and nothing in the UI says so), #8.

## Fixed or obsolete, still open on GitHub

These could be closed with a comment giving the evidence. None has been closed yet.

| # | verdict | evidence at `1f8cb02` |
|---|---|---|
| #12 | fixed | `worthiness.classify_expr` walks the live expression; the regex classifier is gone. |
| #77 | fixed | Snapshots are named by content hash and read through `${TALLYMAN_PROJECT_ROOT}`. A project copied to a new home reads and heals. The rest is #209. |
| #132 | fixed | The `entry_cache` route exists, CI builds the SPA, and the bundle warns when its revision differs from the server's. |
| #133 | fixed | The detail request no longer touches Buckaroo, and the grid tab shows a spinner, then the grid or the error with a retry. |
| #134 | fixed | The metadata tab shows imported bytes, last-modified, the cheap or expensive label with its reason, and parent and child links. |
| #135 | fixed | `catalog_revise` refuses a recipe that names its own alias, and parent references render as links. |
| #141 | fixed | A named schema that misses the header raises; a tuple schema binds by position. |
| #143 | fixed | Inference escalates from 100 rows to 10k to the whole file, and a failed pinned type suggests a schema. |
| #144 | fixed | `time`, `decimal` (as float64) and non-nullable types map. |
| #145 | fixed | The zone and precision are kept. The offset-less text case was #231, since fixed (below). |
| #89 | mostly fixed | Scan, recalc, auto-recalc and their tools shipped. Only the archive overlay is unbuilt. |
| #168 | obsolete | ADR-011 removed the mechanism: `tallyman_read_csv` is refused in recipes, and an edited file is imported as a new version. |
| #147 | obsolete | A UDF makes an entry worthy, and a worthy entry is canonically sorted. |
| #117 | obsolete | Snapshots are named by content hash, so a UDF entry has one stable path. |
| #98 | obsolete | The native catalog store replaced xorq's catalog; `assert_consistency` is no longer called. |
| #30 | obsolete | "Worthy" is a row-order correctness rule (ADR-008 D4), not a cost choice. Eviction is #10. |
| #31 | obsolete | ADR-007 D6 (tallyman hands Buckaroo a view build over the snapshot) settles the cache-hint protocol. |
| #28 | obsolete | The endpoint exists only on unmerged PR #26. |

## Closed in this pass

Closed with a comment linking the fix, on 2026-09-24 unless noted. Each fix is on #189's
branch and reaches `main` with #189.

- **Fixed:**
  - #193 (a failed build deleted the snapshot on disk; fixed by #222).
  - #194, #195, #196 (reset and banner dismissal lost the pin; fixed by #223).
  - #197, #198 (the ordered copy changed types and rewrote reader options; fixed by ADR-011).
  - #118 (page reads executed on the shared backend with no lock and failed with `Already borrowed`;
    fixed by #242, closed 2026-09-26).
  - #183 (two tallyman servers on one project went undetected; fixed by #241, closed 2026-09-26).
  - #204 (`cache_worthy` fell back to "a snapshot exists" when the manifest was missing; fixed by
    #245, closed 2026-09-26).
  - #231 (a zoned timestamp in an import schema read offset-less text as UTC and converted it; fixed
    by #244, closed 2026-09-26).
- **Obsolete** (ADR-011 removed the mechanism): #191, #207, #211.
